package com.example.demo.repositories;

import org.springframework.data.jpa.repository.JpaRepository;

import model.Misici;

public interface MisiciRepository extends JpaRepository<Misici, Integer> {
	
	
}